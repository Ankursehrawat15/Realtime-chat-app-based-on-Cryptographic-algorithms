<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.securechit_chat">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_securechitchat"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_securechitchat"
        android:supportsRtl="true"
        android:theme="@style/Theme.SecureChitChat">
        <activity
            android:name=".activities.NewUsersChat"
            android:exported="true" />
        <activity
            android:name=".activities.MessagesList"
            android:exported="true" />
        <activity
            android:name=".activities.SignIn"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.SignUp"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/splashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>